# Set your local timezone (django will try to figure this out automatically)
TIME_ZONE = 'Europe/Paris'

# Override the default 60 seconds cache duration to get a 'realtime' Grafana
DEFAULT_CACHE_DURATION = 1
